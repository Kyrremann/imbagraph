#menu.custom-wrapper.pure-g
  .pure-u
    .pure-menu
      %a.pure-menu-heading.custom-brand{href: "/"} Imbagraph
      - if locals[:user]
        %a#toggle.custom-toggle{href: "#"}
          %s.bar
          %s.bar
  - if locals[:user]
    .pure-u-1.pure-u-md-1-3
      .pure-menu.pure-menu-horizontal.custom-can-transform
        %ul.pure-menu-list
          %li.pure-menu-item
            %a.pure-menu-link{href: "/stats/#{user.username}/"} Stats
          %li.pure-menu-item
            %a.pure-menu-link{href: "/stats/#{user.username}/beers"} Beers
          %li.pure-menu-item
            %a.pure-menu-link{href: "/stats/#{user.username}/breweries"} Breweries
          %li.pure-menu-item
            %a.pure-menu-link{href: "/stats/#{user.username}/tagged"} Tagged
          %li.pure-menu-item
            %a.pure-menu-link{href: "/stats/#{user.username}/upload"} Upload
